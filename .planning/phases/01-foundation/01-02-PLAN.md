---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "As classes `bg-dne-mint` e `bg-dne-navy` funcionam como utilitarios Tailwind em qualquer componente"
    - "O token `--color-dne-mint: #8CD6BF` esta definido em `@theme inline` em globals.css"
    - "O token `--color-dne-navy: #1B3A6B` esta definido em `@theme inline` em globals.css"
    - "A fonte Nunito esta carregada via next/font/google com variavel CSS `--font-nunito` disponivel no body"
    - "O build de producao nao tem warnings de font ou erros de @theme"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tokens DNE brand (dne-mint, dne-navy, dne-text) no bloco @theme inline existente"
      contains: "--color-dne-mint"
    - path: "src/app/layout.tsx"
      provides: "Nunito carregado com variable: '--font-nunito', aplicado ao body"
      exports: ["RootLayout", "metadata"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "nunito.variable em className do body"
      pattern: "nunito\\.variable"
    - from: "src/app/globals.css"
      to: "Tailwind v4"
      via: "@theme inline block"
      pattern: "--color-dne-mint"
---

<objective>
Definir os tokens CSS de brand DNE no bloco `@theme inline` existente em globals.css e carregar a fonte Nunito via next/font/google em layout.tsx.

Purpose: Criar a base de design tokens que todos os componentes visuais da Phase 2 usarao. A fonte Nunito e necessaria para o logo "dne" na Phase 2; os tokens de cor evitam valores hex inline espalhados pelo codigo.
Output: globals.css com tokens `--color-dne-mint`, `--color-dne-navy`, `--color-dne-text`; layout.tsx com Nunito carregado e variavel CSS `--font-nunito` disponivel globalmente.
</objective>

<execution_context>
@/Users/brunolago/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Estado atual dos arquivos que serao modificados. Executor usa diretamente — sem exploracao. -->

From src/app/globals.css (ESTADO ATUAL — bloco @theme inline sera ESTENDIDO, nao substituido):
```css
@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}
```

From src/app/layout.tsx (ESTADO ATUAL — Nunito sera ADICIONADO):
```typescript
import { Geist, Geist_Mono } from "next/font/google";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

// No body:
<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
```

ESTADO ALVO de globals.css (bloco @theme inline):
```css
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);

  /* DNE brand tokens */
  --color-dne-mint: #8CD6BF;
  --color-dne-navy: #1B3A6B;
  --color-dne-text: #5C5C5C;

  /* Nunito font token */
  --font-nunito: var(--font-nunito);
}
```

ESTADO ALVO de layout.tsx:
```typescript
import { Geist, Geist_Mono, Nunito } from "next/font/google";

const nunito = Nunito({
  subsets: ["latin"],
  variable: "--font-nunito",
  display: "swap",
  // Sem weight — Nunito e variable font no Google Fonts
});

// No body:
<body className={`${geistSans.variable} ${geistMono.variable} ${nunito.variable} antialiased`}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tokens DNE ao @theme inline em globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Modificar o bloco `@theme inline {}` existente em globals.css adicionando os tokens de brand DNE logo apos os tokens existentes.

    REGRA CRITICA: Adicionar dentro do bloco `@theme inline {}` EXISTENTE — NAO criar um segundo bloco `@theme`. Um segundo bloco sem `inline` muda a semantica de resolucao das variaveis CSS ja definidas.

    Resultado final de globals.css:
    ```css
    @import "tailwindcss";

    :root {
      --background: #ffffff;
      --foreground: #171717;
    }

    @theme inline {
      --color-background: var(--background);
      --color-foreground: var(--foreground);
      --font-sans: var(--font-geist-sans);
      --font-mono: var(--font-geist-mono);

      /* DNE brand tokens */
      --color-dne-mint: #8CD6BF;
      --color-dne-navy: #1B3A6B;
      --color-dne-text: #5C5C5C;

      /* Nunito font token */
      --font-nunito: var(--font-nunito);
    }

    body {
      background: var(--background);
      color: var(--foreground);
      font-family: Arial, Helvetica, sans-serif;
    }
    ```

    Apos esta mudanca, as classes Tailwind `bg-dne-mint`, `bg-dne-navy`, `text-dne-text` e `font-nunito` estarao disponiveis em todo o projeto.
  </action>
  <verify>
    <automated>grep -c "dne-mint\|dne-navy\|dne-text\|font-nunito" /Users/brunolago/Developer/dne-digital/src/app/globals.css</automated>
  </verify>
  <done>globals.css contem os 4 tokens DNE dentro do unico bloco @theme inline (grep retorna 4)</done>
</task>

<task type="auto">
  <name>Task 2: Carregar Nunito via next/font/google em layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <action>
    Modificar layout.tsx para adicionar Nunito ao lado dos fonts Geist existentes.

    1. Adicionar `Nunito` ao import existente de next/font/google:
    ```typescript
    import { Geist, Geist_Mono, Nunito } from "next/font/google";
    ```

    2. Adicionar a configuracao de Nunito apos geistMono (usar `const`, nao `var`):
    ```typescript
    const nunito = Nunito({
      subsets: ["latin"],
      variable: "--font-nunito",
      display: "swap",
    });
    ```
    Sem o campo `weight` — Nunito e variable font no Google Fonts, aceita qualquer peso via CSS.
    Obrigatorio: `subsets: ["latin"]` (sem isso, Next.js emite warning).

    3. Adicionar `nunito.variable` ao className do body:
    ```tsx
    <body
      className={`${geistSans.variable} ${geistMono.variable} ${nunito.variable} antialiased`}
    >
    ```

    O resultado e que a variavel CSS `--font-nunito` fica disponivel em todo o document. A Phase 2 aplicara `font-nunito` ou `style={{ fontFamily: 'var(--font-nunito)' }}` apenas no elemento `<h1>` do logo.

    NAO aplicar Nunito no body — o body deve continuar com a fonte padrao do sistema. Nunito e apenas para o logo "dne".
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `pnpm exec tsc --noEmit` retorna sem erros
    - layout.tsx importa Nunito de next/font/google
    - layout.tsx define `const nunito = Nunito({ subsets: ["latin"], variable: "--font-nunito", display: "swap" })`
    - nunito.variable esta no className do body
  </done>
</task>

</tasks>

<verification>
Verificacao geral do plan apos execucao:

1. `pnpm exec tsc --noEmit` — zero erros TypeScript
2. `pnpm build` — build de producao sem warnings de font
3. Verificar que o token aparece no CSS compilado:
   - Em dev, abrir DevTools -> Elements -> :root ou o elemento body -> confirmar `--font-nunito` na lista de custom properties
</verification>

<success_criteria>
- `bg-dne-mint`, `bg-dne-navy`, `text-dne-text` sao classes Tailwind validas (verificavel em build)
- `--font-nunito` esta disponivel como CSS custom property no documento
- TypeScript compila sem erros
- Nenhum segundo bloco `@theme` foi criado (grep 'theme' globals.css retorna exatamente 1 ocorrencia de `@theme inline`)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-02-SUMMARY.md` com:
- Tokens adicionados ao @theme inline
- Resultado do build (warnings de font, se houver)
- Confirmacao de que o bloco @theme inline permanece unico
</output>
