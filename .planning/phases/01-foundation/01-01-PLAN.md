---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/constants/index.ts
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - CARD-04
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "O QR code aparece na carteirinha sem qualquer chamada de rede para api.qrserver.com"
    - "O TypeScript compila sem erros — nenhuma referencia a data.qrcode no codigo"
    - "Os campos 'Nivel de Ensino' e 'RG' nao aparecem no InfoCard"
    - "A interface Student contem exatamente 8 campos (nome, instituicao, curso, cpf, nascimento, validade, codigoCie, foto)"
  artifacts:
    - path: "src/constants/index.ts"
      provides: "Interface Student limpa (8 campos) + DATABASE sem nivel/rg/qrcode"
      exports: ["Student", "DATABASE", "STUDENT_IDS"]
    - path: "src/app/[student]/page.tsx"
      provides: "QRCodeSVG renderizado a partir de data.codigoCie, sem referencias a data.qrcode, data.nivel, data.rg"
  key_links:
    - from: "src/app/[student]/page.tsx"
      to: "src/constants/index.ts"
      via: "import { DATABASE } from '@/constants'"
      pattern: "QRCodeSVG.*codigoCie"
    - from: "src/app/[student]/page.tsx"
      to: "qrcode.react"
      via: "import { QRCodeSVG } from 'qrcode.react'"
      pattern: "QRCodeSVG"
---

<objective>
Instalar qrcode.react e realizar a mudanca atomica: substituir a imagem externa de QR por QRCodeSVG local, ao mesmo tempo que o tipo Student e o DATABASE sao limpos (remocao de nivel, rg, qrcode).

Purpose: Eliminar a dependencia critica de demo em api.qrserver.com e deixar o modelo de dados alinhado com o app original DNE. As duas mudancas DEVEM ser feitas na mesma execucao para evitar erros TypeScript de referencia a campos removidos.
Output: qrcode.react instalado, QR gerado localmente, tipo Student com 8 campos, DATABASE sem campos obsoletos, InfoCard exibindo apenas os 5 campos corretos.
</objective>

<execution_context>
@/Users/brunolago/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Estado atual dos arquivos que serao modificados. Executor usa diretamente — sem exploracao. -->

From src/constants/index.ts (ESTADO ATUAL — sera substituido):
```typescript
export type Student = {
  nome: string;
  instituicao: string;
  curso: string;
  nivel: string;        // REMOVER
  rg: string;           // REMOVER
  cpf: string;
  nascimento: string;
  validade: string;
  codigoCie: string;
  foto: string;
  qrcode: string;       // REMOVER — QR agora gerado localmente de codigoCie
};

export const DATABASE: Record<string, Student> = {
  luccas: { ..., nivel: "SUPERIOR", rg: "159124908", ..., qrcode: "https://api.qrserver.com/..." },
  joao:   { ..., nivel: "SUPERIOR", rg: "398004705", ..., qrcode: "https://api.qrserver.com/..." },
};
```

From src/app/[student]/page.tsx (trecho relevante — linha 77):
```tsx
<Image
  src={data.qrcode}        // SUBSTITUIR por QRCodeSVG
  alt="QR Code"
  width={200}
  height={200}
  className="w-[95%] mix-blend-multiply"
/>
```
InfoRows a remover (linhas 106-107):
```tsx
<InfoRow label="Nivel de Ensino" value={data.nivel} />   // REMOVER
<InfoRow label="RG" value={data.rg} />                   // REMOVER
```

ESTADO ALVO de Student (interface):
```typescript
export interface Student {
  nome: string;
  instituicao: string;
  curso: string;
  cpf: string;
  nascimento: string;
  validade: string;
  codigoCie: string;
  foto: string;
}
```

QRCodeSVG — API verificada (qrcode.react v4.2.0):
```typescript
import { QRCodeSVG } from 'qrcode.react';
// Props: value (string), size (number), bgColor (string), fgColor (string),
//        level ("L"|"M"|"Q"|"H"), marginSize (number — modulos de quiet zone)
// NAO usar: includeMargin (deprecated v4)
// Nao precisa de "use client" para render puro (Phase 1 — sem clipboard)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar qrcode.react</name>
  <files>package.json</files>
  <action>
    Rodar na raiz do projeto:
    ```bash
    pnpm add qrcode.react
    ```
    Verificar que `"qrcode.react"` aparece em `dependencies` no package.json apos o comando.
    Nao adicionar `@types/qrcode.react` — a biblioteca inclui tipos proprios.
  </action>
  <verify>
    <automated>grep '"qrcode.react"' /Users/brunolago/Developer/dne-digital/package.json</automated>
  </verify>
  <done>package.json contem "qrcode.react" em dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Limpar Student type + DATABASE e substituir QR atomicamente</name>
  <files>
    src/constants/index.ts
    src/app/[student]/page.tsx
  </files>
  <action>
    Esta task DEVE modificar os dois arquivos em sequencia sem parar. Sao mudancas acopladas — qualquer estado intermediario causa erro TypeScript.

    **1. src/constants/index.ts — reescrever completamente:**

    ```typescript
    export interface Student {
      nome: string;
      instituicao: string;
      curso: string;
      cpf: string;
      nascimento: string;
      validade: string;
      codigoCie: string;
      foto: string;
    }

    export const DATABASE: Record<string, Student> = {
      luccas: {
        nome: "Luccas Salvagni Queiroz Santos",
        instituicao: "Faculdade De Informatica e Administracao",
        curso: "Ciência da Computação",
        cpf: "05369802254",
        nascimento: "03/11/2000",
        validade: "31/03/2026",
        codigoCie: "09ZB5S",
        foto: "https://media.discordapp.net/attachments/1018609889985187911/1441860148632092884/99a243e6-2fdb-4741-a7bc-e5e89f2b5a4b.JPG?ex=69235467&is=692202e7&hm=4979bb6cfa4eb01a8f1af76f4b84fa6b0e3300d7b8d0e60012a804abf4453c4b&=&format=webp&width=733&height=977",
      },
      joao: {
        nome: "João Pedro Militão da Silva",
        instituicao: "Mackenzie",
        curso: "Direito",
        cpf: "39456076806",
        nascimento: "12/10/2002",
        validade: "31/04/2026",
        codigoCie: "10XC8T",
        foto: "https://media.discordapp.net/attachments/1018609889985187911/1441860148632092884/99a243e6-2fdb-4741-a7bc-e5e89f2b5a4b.JPG?ex=69235467&is=692202e7&hm=4979bb6cfa4eb01a8f1af76f4b84fa6b0e3300d7b8d0e60012a804abf4453c4b&=&format=webp&width=733&height=977",
      },
    };

    export const STUDENT_IDS = Object.keys(DATABASE);
    ```

    Nota: usar `interface` (nao `type`) conforme CLAUDE.md. Typo "Adminstracao" corrigido para "Administracao".

    **2. src/app/[student]/page.tsx — tres mudancas:**

    a) Adicionar import de QRCodeSVG no topo (apos os imports existentes):
    ```typescript
    import { QRCodeSVG } from 'qrcode.react';
    ```

    b) Substituir o bloco `<Image src={data.qrcode} ...>` (linha ~77) por:
    ```tsx
    <QRCodeSVG
      value={data.codigoCie}
      size={160}
      bgColor="#FFFFFF"
      fgColor="#000000"
      level="M"
      marginSize={1}
      className="w-[95%] mix-blend-multiply"
    />
    ```

    c) Remover as duas linhas de InfoRow para campos removidos:
    ```tsx
    // REMOVER estas duas linhas:
    <InfoRow label="Nivel de Ensino" value={data.nivel} />
    <InfoRow label="RG" value={data.rg} />
    ```

    Nao adicionar "use client" ao page.tsx — QRCodeSVG renderiza como RSC em Phase 1 (sem interatividade de clipboard). Se o compilador Next.js lancar erro de limite RSC/client ao importar qrcode.react, criar um componente minimo `src/shared/components/QrCode.tsx` com `"use client"` no topo e mover o import + JSX do QRCodeSVG para ele. Isso e uma contingencia — tente primeiro sem.
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `pnpm exec tsc --noEmit` retorna sem erros
    - `src/constants/index.ts` nao contem "nivel", "rg" ou "qrcode"
    - `src/app/[student]/page.tsx` nao contem `data.qrcode`, `data.nivel` ou `data.rg`
    - `src/app/[student]/page.tsx` contem `QRCodeSVG` com prop `value={data.codigoCie}`
  </done>
</task>

</tasks>

<verification>
Verificacao geral da phase apos execucao:

1. `pnpm exec tsc --noEmit` — zero erros TypeScript
2. `pnpm build` — build de producao completa sem erros
3. Verificar manualmente no browser (`pnpm dev`, acessar `/luccas`): QR code renderiza inline sem chamada de rede para api.qrserver.com (verificar Network tab — nenhuma requisicao para api.qrserver.com)
</verification>

<success_criteria>
- QR code renderiza localmente em `/luccas` e `/joao` sem acesso a internet
- TypeScript compila sem erros (`pnpm exec tsc --noEmit`)
- InfoCard exibe: Ins. Ensino, Curso, CPF, Data de Nasc, Validade (sem Nivel de Ensino, sem RG)
- Student interface tem exatamente 8 campos
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-01-SUMMARY.md` com:
- O que foi feito (qrcode.react instalado, Student limpo, QR substituido)
- Contingencia usada (se QRCodeSVG precisou de wrapper "use client")
- Estado final dos arquivos modificados
</output>
