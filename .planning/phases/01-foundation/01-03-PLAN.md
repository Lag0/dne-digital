---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - DATA-02

must_haves:
  truths:
    - "O container mobile tem max-width de 390px (iPhone 14) — nao 400px"
    - "O container mobile usa h-[100dvh] para corrigir o clipping no iOS Safari"
    - "Em desktop (md:), o container tem h-[844px] fixo — nao 850px"
    - "O fundo da pagina usa bg-stone-900 (ja correto — confirmar nao foi removido)"
    - "O badge Apple Wallet renderiza com `w-[160px]` — classe Tailwind valida (bug w-[160] corrigido)"
    - "O container usa `bg-dne-mint` (token CSS) em vez do hex inline `bg-[#8CD6BF]`"
  artifacts:
    - path: "src/app/[student]/page.tsx"
      provides: "Container mobile com dimensoes corretas (390px x 100dvh/844px) e token de cor"
      contains: "max-w-[390px]"
  key_links:
    - from: "src/app/[student]/page.tsx"
      to: "src/app/globals.css"
      via: "bg-dne-mint token consumido via Tailwind"
      pattern: "bg-dne-mint"
---

<objective>
Corrigir as dimensoes do container mobile em page.tsx para usar os valores corretos do iPhone 14 (390px x 844px), substituir `h-screen` por `h-[100dvh]` para corrigir o clipping no iOS Safari, usar o token `bg-dne-mint` em vez do hex inline, e corrigir o bug `w-[160]` para `w-[160px]` no badge Apple Wallet.

Purpose: Completar a fundacao visual — frame mobile com proporcoes corretas e sem bugs de classes Tailwind invalidas. Depende do plan 01-01 ter sido executado (page.tsx ja com QRCodeSVG e sem InfoRows de nivel/rg).
Output: page.tsx com container mobile correto e bug de Apple Wallet corrigido.
</objective>

<execution_context>
@/Users/brunolago/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Estado do arquivo APOS execucao do plan 01-01. Executor deve ler o arquivo atual antes de modificar. -->

From src/app/[student]/page.tsx — trechos a modificar:

TRECHO 1 — Container mobile (linha ~25):
```tsx
// ANTES (incorreto):
<div className="w-full max-w-[400px] bg-[#8CD6BF] h-screen md:h-[850px] md:rounded-[40px] overflow-hidden relative flex flex-col shadow-2xl border-0 md:border-8 border-stone-800">

// DEPOIS (correto):
<div className="w-full max-w-[390px] bg-dne-mint h-[100dvh] md:h-[844px] md:rounded-[40px] overflow-hidden relative flex flex-col shadow-2xl border-0 md:border-8 border-stone-800">
```
Mudancas neste trecho:
- `max-w-[400px]` → `max-w-[390px]` (iPhone 14 logical width)
- `bg-[#8CD6BF]` → `bg-dne-mint` (usar token CSS definido em plan 01-02)
- `h-screen` → `h-[100dvh]` (corrige iOS Safari toolbar clipping)
- `md:h-[850px]` → `md:h-[844px]` (iPhone 14 logical height)

TRECHO 2 — Badge Apple Wallet (linha ~132):
```tsx
// ANTES (bug — w-[160] nao e classe Tailwind valida):
className="w-[160] h-[46px]"

// DEPOIS (correto):
className="w-[160px] h-[46px]"
```

IMPORTANTE: plan 01-02 e executado em paralelo ao plan 01-01 (Wave 1). Este plan 01-03 e Wave 2 e assume que o token `bg-dne-mint` ja esta disponivel (definido em globals.css pelo plan 01-02). Se por alguma razao 01-02 nao foi executado, usar `bg-[#8CD6BF]` como fallback temporario e registrar no SUMMARY.

O restante do arquivo (imports, header, QR card com QRCodeSVG, InfoCard, footer) NAO deve ser modificado neste plan — e escopo de Phase 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir dimensoes do container mobile e token de cor</name>
  <files>src/app/[student]/page.tsx</files>
  <action>
    Localizar o div do container mobile (o elemento com `max-w-[400px]`) e realizar as seguintes substituicoes EXATAS:

    1. `max-w-[400px]` → `max-w-[390px]`
    2. `bg-[#8CD6BF]` → `bg-dne-mint`
    3. `h-screen` → `h-[100dvh]`
    4. `md:h-[850px]` → `md:h-[844px]`

    Todas as quatro substituicoes sao no mesmo elemento div (linha ~25 do arquivo apos execucao de 01-01).

    Racional de cada mudanca:
    - 390px: resolucao logica do iPhone 14 (valor correto para o frame de demo)
    - bg-dne-mint: usa o token CSS definido em plan 01-02 em vez de hex inline
    - h-[100dvh]: unidade CSS `dvh` (dynamic viewport height) — corrige o clipping causado pela barra de endereco do iOS Safari que nao e considerada por `100vh`
    - 844px: altura logica do iPhone 14 (correcao de 850px incorreto)

    NAO modificar nenhum outro elemento do arquivo. NAO adicionar "use client". NAO alterar imports.
  </action>
  <verify>
    <automated>grep -c "max-w-\[390px\]\|h-\[100dvh\]\|bg-dne-mint\|md:h-\[844px\]" /Users/brunolago/Developer/dne-digital/src/app/[student]/page.tsx</automated>
  </verify>
  <done>
    - grep retorna 4 (todos os quatro valores corretos presentes)
    - Arquivo NAO contem mais `max-w-[400px]`, `h-screen` (em classes), `bg-[#8CD6BF]` ou `md:h-[850px]`
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir bug w-[160] no badge Apple Wallet</name>
  <files>src/app/[student]/page.tsx</files>
  <action>
    Localizar a imagem do Apple Wallet badge (elemento Image com `alt="Apple Wallet"`) e corrigir a classe Tailwind invalida.

    Mudanca:
    ```tsx
    // ANTES (bug — Tailwind nao gera CSS para w-[160] sem unidade):
    className="w-[160] h-[46px]"

    // DEPOIS (correto):
    className="w-[160px] h-[46px]"
    ```

    Esta e uma mudanca de uma linha. O Tailwind v4 (como v3) ignora valores arbitrarios sem unidade CSS valida — `w-[160]` nao gera nenhum CSS, fazendo o badge ficar com largura 0 ou herdada. Adicionar `px` corrige o rendering.
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `pnpm exec tsc --noEmit` retorna sem erros
    - Arquivo contem `w-[160px]` (com unidade)
    - Arquivo NAO contem `w-[160]` sem unidade (verificar com grep que retorna 0 ocorrencias de `w-\[160\][^p]`)
  </done>
</task>

</tasks>

<verification>
Verificacao geral do plan apos execucao:

1. `pnpm exec tsc --noEmit` — zero erros
2. `pnpm build` — build de producao sem erros
3. Verificacao visual (pnpm dev, abrir /luccas):
   - Em mobile (390px): carteirinha preenche a tela inteiramente sem clipping da barra de endereco
   - Em desktop: frame de celular aparece centralizado com fundo stone-900, dimensoes ~390x844px
   - Badge Apple Wallet renderiza com largura correta (nao colapsado)
4. `grep "max-w-\[390px\]" src/app/[student]/page.tsx` retorna resultado
5. `grep "h-\[100dvh\]" src/app/[student]/page.tsx` retorna resultado
</verification>

<success_criteria>
- Container mobile: max-w-[390px], h-[100dvh], md:h-[844px], bg-dne-mint
- Apple Wallet badge: w-[160px] (sem bug de unidade ausente)
- TypeScript compila sem erros
- Build de producao conclui sem erros
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-03-SUMMARY.md` com:
- Mudancas realizadas (4 substituicoes no container + 1 no badge)
- Confirmacao do build
- Nota se o token bg-dne-mint estava disponivel (plan 01-02 executado) ou se foi necessario usar fallback
</output>
