---
plan: 03-01
phase: 03-polish
title: "PhotoCard fallback local → CDN"
wave: 1
depends_on: []
files_modified:
  - src/components/photo-card.tsx
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - CARD-03
---

# Plan 03-01: PhotoCard fallback local → CDN

## Goal

`PhotoCard` tenta carregar `/photos/{studentId}.jpeg` do `public/` local primeiro. Se o arquivo nao existir, cai graciosamente para a URL externa (CDN) contida em `data.foto`. Nenhum erro visivel ao usuario.

## Context

**Current state:**
- `photo-card.tsx` e um RSC puro: `({ src, alt }) => <Image src={src} ... />` sem `onError`
- `page.tsx` passa `src={data.foto}` que e uma URL CDN para `luccas`/`joao` e um path local (`/hebert.jpeg`, `/richards.jpeg`) para `herbert`/`richard`
- `public/` tem `hebert.jpeg` e `richards.jpeg` diretamente na raiz (sem subpasta `photos/`)
- Nao existe `/public/photos/` — os paths locais em DATABASE sao `/hebert.jpeg` e `/richards.jpeg` (raiz de `public/`)

**Target state:**
- `PhotoCard` aceita `src` (local path) e `fallbackSrc` (CDN URL)
- Se `src` falhar a carregar, `onError` troca para `fallbackSrc` com guard anti-loop
- `page.tsx` passa `src="/photos/{student}.jpeg"` (path local padrao) e `fallbackSrc={data.foto}`
- `onError` so dispara se `imgSrc !== fallbackSrc` (evita loop infinito)
- Estudantes sem foto local (`luccas`, `joao`) tem `onError` disparado imediatamente e carregam CDN normalmente

**Key decisions from STATE.md:**
- `[Phase 02-visual-components]`: PhotoCard usa fill em vez de width/height — manter
- `[DATA-02]`: Campo `foto` suporta path local ou URL externa — confirma a arquitetura

## Tasks

<tasks>
  <task id="03-01-T1">
    <title>Converter PhotoCard para Client Component com onError fallback</title>
    <file>src/components/photo-card.tsx</file>
    <instructions>
      1. Adicionar `'use client'` como primeira linha do arquivo
      2. Adicionar import: `import { useState } from 'react'`
      3. Atualizar a interface `PhotoCardProps` para:
         ```typescript
         interface PhotoCardProps {
           src: string;
           fallbackSrc: string;
           alt: string;
         }
         ```
      4. Converter o componente de arrow function constante para usar `useState`:
         - Usar `const [imgSrc, setImgSrc] = useState(src)` para rastrear src atual
         - Adicionar `onError` no `<Image>` com guard anti-loop:
           ```typescript
           onError={() => {
             if (imgSrc !== fallbackSrc) setImgSrc(fallbackSrc)
           }}
           ```
      5. Atualizar o JSDoc para documentar CARD-03 e o comportamento de fallback
      6. Manter todas as outras props (`fill`, `sizes`, `className`) identicas ao estado atual

      **Resultado esperado:**
      ```typescript
      'use client'

      import Image from 'next/image'
      import { useState } from 'react'

      interface PhotoCardProps {
        src: string
        fallbackSrc: string
        alt: string
      }

      /**
       * Card da foto do estudante com fallback local → CDN.
       * CARD-03: tenta src local primeiro; onError troca para fallbackSrc se ausente.
       * Parent externo tem w-1/2 e o container pai tem h-[280px] — fill herda essas dimensoes.
       */
      export const PhotoCard = ({ src, fallbackSrc, alt }: PhotoCardProps) => {
        const [imgSrc, setImgSrc] = useState(src)

        return (
          <div className="bg-white rounded-xl p-1.5 shadow-sm w-1/2 relative">
            <div className="relative w-full h-full rounded-lg overflow-hidden">
              <Image
                src={imgSrc}
                alt={alt}
                fill
                sizes="195px"
                className="object-cover"
                onError={() => {
                  if (imgSrc !== fallbackSrc) setImgSrc(fallbackSrc)
                }}
              />
            </div>
          </div>
        )
      }
      ```
    </instructions>
  </task>

  <task id="03-01-T2">
    <title>Atualizar call site de PhotoCard em page.tsx</title>
    <file>src/app/[student]/page.tsx</file>
    <instructions>
      1. Localizar a linha com `<PhotoCard src={data.foto} alt="Foto do estudante" />`
      2. Substituir por:
         ```tsx
         <PhotoCard
           src={`/photos/${student}.jpeg`}
           fallbackSrc={data.foto}
           alt="Foto do estudante"
         />
         ```
         - `src` usa template literal com o `student` param (ja disponivel como `const { student } = await params`)
         - `fallbackSrc` e `data.foto` (a URL CDN ou path local existente no DATABASE)
      3. Nenhuma outra mudanca em `page.tsx`

      **Logica de fallback resultante por estudante:**
      - `luccas`: `src="/photos/luccas.jpeg"` (nao existe) → onError → `fallbackSrc=CDN URL` ✓
      - `joao`: `src="/photos/joao.jpeg"` (nao existe) → onError → `fallbackSrc=CDN URL` ✓
      - `herbert`: `src="/photos/herbert.jpeg"` (nao existe) → onError → `fallbackSrc="/hebert.jpeg"` (existe em public/) ✓
      - `richard`: `src="/photos/richard.jpeg"` (nao existe) → onError → `fallbackSrc="/richards.jpeg"` (existe em public/) ✓
    </instructions>
  </task>
</tasks>

## Verification

```
VERIFICAR apos implementacao:
1. TypeScript compila sem erros: `bun run build` (ou `next build`)
2. Nenhum erro "Event handlers cannot be passed to Client Component props" no build
3. Em http://localhost:3000/luccas — foto carrega (via CDN apos onError)
4. Em http://localhost:3000/herbert — foto carrega (via /hebert.jpeg apos onError)
5. Console do browser nao mostra erros de imagem alem do 404 esperado para /photos/*.jpeg
6. Sem loop infinito de re-renders no DevTools
```

## must_haves

- `PhotoCard` tem `'use client'` como primeira diretiva
- Interface `PhotoCardProps` inclui campo `fallbackSrc: string`
- `onError` handler tem guard `if (imgSrc !== fallbackSrc)` para evitar loop
- `page.tsx` passa `src` como `/photos/${student}.jpeg` e `fallbackSrc` como `data.foto`
- TypeScript compila sem erros (sem uso de `any`)
- Comportamento de fallback: foto aparece em todos os estudantes (local ou CDN)
