---
plan: 03-02
phase: 03-polish
title: "QrCard clipboard interativo + verificar FOOT-03"
wave: 1
depends_on: []
files_modified:
  - src/components/qr-card.tsx
  - src/components/card-footer.tsx
autonomous: true
requirements:
  - CARD-06
  - FOOT-03
---

# Plan 03-02: QrCard clipboard interativo + verificar FOOT-03

## Goal

`QrCard` vira Client Component com `navigator.clipboard` funcional: clicar no codigo CIE copia para o clipboard e exibe icone `Check` verde por 2 segundos, voltando ao `Copy` cinza. FOOT-03 e verificado e confirmado como ja corrigido (ou corrigido aqui se necessario).

## Context

**Current state — QrCard:**
- RSC puro (sem `'use client'`), componente estateless como arrow function
- Icone `Copy` presente mas sem `onClick` — e um placeholder visual (comentado em JSDoc: "funcionalidade de clipboard em Phase 3 — CARD-06")
- O `<div>` que agrupa codigo+icone nao e um `<button>` — nao e clicavel

**Current state — CardFooter (FOOT-03):**
- `card-footer.tsx` linha 30: `className="w-[160px] h-[46px]"` — `w-[160px]` JA esta correto
- O bug `w-[160]` foi corrigido anteriormente (possivelmente em 01-03-PLAN)
- REQUIREMENTS.md ainda marca FOOT-03 como pending — necessita apenas confirmacao
- `width={220}` intrinsico no `<Image>` e inconsistente com `w-[160px]` do className; o className prevalece visualmente mas os dois deveriam ser coerentes — ajustar `width={160}` para alinhamento semantico

**Key decisions from STATE.md:**
- `[Phase 02-visual-components]`: QrCard RSC puro — Copy e placeholder visual, clipboard interativo em Phase 3 (CARD-06) — confirma que esta e a fase certa
- `[01-01]`: QRCodeSVG sem "use client" — renderiza como RSC em Phase 1 sem interatividade. Agora em Phase 3 adicionamos "use client" deliberadamente

## Tasks

<tasks>
  <task id="03-02-T1">
    <title>Converter QrCard para Client Component com clipboard interativo</title>
    <file>src/components/qr-card.tsx</file>
    <instructions>
      1. Adicionar `'use client'` como primeira linha do arquivo
      2. Adicionar imports: `import { useState } from 'react'` e `import { Copy, Check } from 'lucide-react'`
         (Check ainda nao esta importado no arquivo atual — adicionar)
      3. Adicionar `useState<boolean>` para o estado `isCopied`
      4. Criar handler `handleCopy` com try/catch:
         ```typescript
         const handleCopy = async () => {
           try {
             await navigator.clipboard.writeText(codigoCie)
             setIsCopied(true)
             setTimeout(() => setIsCopied(false), 2000)
           } catch {
             // clipboard API indisponivel — sem feedback, sem erro silencioso critico
           }
         }
         ```
      5. Converter o `<div className="flex items-center gap-1 ...">` que contem codigo+icone para um `<button>`:
         - `onClick={handleCopy}`
         - `aria-label="Copiar código CIE"`
         - Manter `className="flex items-center gap-1 text-stone-800 font-bold text-sm"`
      6. Trocar o icone: quando `isCopied === true` renderizar `<Check size={12} className="text-green-500 ml-1" />`, caso contrario `<Copy size={12} className="text-gray-400 ml-1" />`
      7. Atualizar o JSDoc para remover a nota de placeholder e documentar CARD-06

      **Resultado esperado:**
      ```typescript
      'use client'

      import { useState } from 'react'
      import { Copy, Check } from 'lucide-react'
      import { QRCodeSVG } from 'qrcode.react'

      interface QrCardProps {
        codigoCie: string
      }

      /**
       * Card do QR code com clipboard interativo.
       * CARD-06: copia codigoCie para clipboard e exibe feedback Check verde por 2s.
       * QR gerado localmente via qrcode.react — sem dependencia de CDN externo.
       */
      export const QrCard = ({ codigoCie }: QrCardProps) => {
        const [isCopied, setIsCopied] = useState(false)

        const handleCopy = async () => {
          try {
            await navigator.clipboard.writeText(codigoCie)
            setIsCopied(true)
            setTimeout(() => setIsCopied(false), 2000)
          } catch {
            // clipboard API indisponivel
          }
        }

        return (
          <div className="bg-white rounded-xl py-1 shadow-sm w-1/2 flex flex-col items-center justify-center text-center relative">
            <QRCodeSVG
              value={codigoCie}
              size={160}
              bgColor="#FFFFFF"
              fgColor="#000000"
              level="M"
              marginSize={1}
              className="w-[95%] mix-blend-multiply"
            />
            <div className="mt-2 flex flex-col items-center">
              <span className="text-[10px] text-gray-500 font-medium uppercase tracking-wide">
                Nº da CIE
              </span>
              <button
                onClick={handleCopy}
                className="flex items-center gap-1 text-stone-800 font-bold text-sm"
                aria-label="Copiar código CIE"
              >
                {codigoCie}
                {isCopied
                  ? <Check size={12} className="text-green-500 ml-1" />
                  : <Copy size={12} className="text-gray-400 ml-1" />
                }
              </button>
            </div>
          </div>
        )
      }
      ```
    </instructions>
  </task>

  <task id="03-02-T2">
    <title>Verificar e corrigir FOOT-03 em CardFooter</title>
    <file>src/components/card-footer.tsx</file>
    <instructions>
      1. Verificar o estado atual do `<Image>` do badge Apple Wallet:
         - Linha atual: `className="w-[160px] h-[46px]"` — JA correto (w-[160px] com unidade)
         - Linha atual: `width={220}` — INCONSISTENTE com w-[160px] visual; ajustar para `width={160}`
         - Linha atual: `height={35}` — pode ser mantido pois `h-[46px]` no className sobrescreve
      2. Correcao necessaria: alterar `width={220}` para `width={160}` no `<Image>` do badge
         para tornar o prop intrinsico coerente com o className visual
      3. Nao fazer nenhuma outra mudanca no arquivo

      **Estado apos correcao:**
      ```tsx
      <Image
        src={AppleWalletImage}
        alt="Apple Wallet"
        width={160}
        height={46}
        className="w-[160px] h-[46px]"
      />
      ```
      Nota: ajustar `height={35}` para `height={46}` tambem para coerencia total com className.

      **FOOT-03 resultado:** bug `w-[160]` estava ja corrigido para `w-[160px]`; esta task
      finaliza a coerencia semantica entre props intrinsicos e className, fechando FOOT-03 definitivamente.
    </instructions>
  </task>
</tasks>

## Verification

```
VERIFICAR apos implementacao:
1. TypeScript compila sem erros: `bun run build` (ou `next build`)
2. Em http://localhost:3000/luccas:
   a. Clicar no codigo CIE (ex: "09ZB5S") — verificar que e clicavel (cursor pointer)
   b. Apos click: icone muda de Copy (cinza) para Check (verde) imediatamente
   c. Apos 2 segundos: icone volta para Copy (cinza) automaticamente
3. Inspecionar network no DevTools: nenhuma requisicao de clipboard-related
4. Badge Apple Wallet visualmente correto: largura ~160px, sem stretch
5. Sem erros no console do browser alem dos 404 esperados para /photos/*.jpeg
```

## must_haves

- `QrCard` tem `'use client'` como primeira diretiva
- `handleCopy` usa `navigator.clipboard.writeText` com try/catch (sem swallow silencioso critico)
- Icone alterna: `Check` (verde) quando `isCopied === true`, `Copy` (cinza) caso contrario
- Timeout de 2000ms reseta `isCopied` para `false`
- O elemento clicavel e um `<button>` semantico com `aria-label`
- `card-footer.tsx`: props `width` e `height` do `<Image>` coerentes com `className` (`width={160}`, `height={46}`)
- TypeScript compila sem erros (sem uso de `any`)
- FOOT-03 marcado como resolvido (bug w-[160] confirmado ja corrigido + coerencia semantica aplicada)
