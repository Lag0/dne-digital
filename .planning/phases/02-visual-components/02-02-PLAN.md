---
phase: "02-visual-components"
plan: "02-02"
type: standard
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/card-header.tsx
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - HEAD-01
  - HEAD-02
  - HEAD-03
  - HEAD-04
must_haves:
  - "O logo 'dne' usa fontFamily: 'var(--font-nunito)' — sem Arial"
  - "O subtitulo 'Documento Nacional do Estudante' aparece abaixo do logo em texto pequeno branco"
  - "O icone UNE e importado de @/assets/une-icon.svg via SVGR — sem Globe do Lucide"
  - "O icone hamburger (Menu do Lucide) esta presente com cor branca e tamanho correto"
  - "page.tsx importa CardHeader e remove o bloco header inline"
---

# Plan 02-02: Implementar CardHeader

## Objective

Extrair o bloco `<header>` de `page.tsx` para o componente `src/components/card-header.tsx`, aplicando as 4 correcoes visuais: fonte Nunito no logo (HEAD-01), subtitulo "Documento Nacional do Estudante" (HEAD-02), icone UNE via SVG real (HEAD-03), e validar o icone hamburger (HEAD-04).

## Context

- `src/assets/une-icon.svg` ja existe apos 02-01 — pronto para import via SVGR
- `turbopack.rules` configurado em next.config.ts apos 02-01 — import SVG funciona
- `--font-nunito` ja disponivel como CSS variable via layout.tsx (Nunito carregada)
- Header atual em page.tsx (linhas 44-59): logo usa `style={{ fontFamily: "Arial, sans-serif" }}` e Globe do Lucide
- Sem estado, sem interatividade — CardHeader e RSC puro (sem "use client")

## Tasks

<task id="02-02-T1" type="auto">
  <name>Criar componente CardHeader com todas as correcoes visuais</name>
  <files>
    - src/components/card-header.tsx
  </files>
  <action>
    Criar src/components/card-header.tsx como RSC (sem "use client"):

    ```typescript
    import { Menu } from 'lucide-react';
    import UneIcon from '@/assets/une-icon.svg';

    /**
     * Header da carteirinha DNE com logo, subtitulo, icone UNE e hamburger.
     * RSC — sem interatividade.
     */
    export const CardHeader = () => (
      <header className="px-3 pt-2 pb-4 flex justify-between items-center">
        {/* HEAD-01 + HEAD-02: Logo com fonte Nunito e subtitulo */}
        <div className="flex flex-col">
          <h1
            className="text-white text-4xl font-extrabold tracking-tighter select-none leading-none"
            style={{ fontFamily: 'var(--font-nunito)' }}
          >
            dne
          </h1>
          <span className="text-white text-[10px] font-medium leading-tight">
            Documento Nacional do Estudante
          </span>
        </div>

        <div className="flex items-center gap-4">
          {/* HEAD-03: Icone UNE como SVG real — substituindo Globe do Lucide */}
          <div className="w-8 h-8 bg-white rounded-full flex items-center justify-center overflow-hidden">
            <UneIcon className="w-7 h-7" />
          </div>

          {/* HEAD-04: Icone hamburger — Menu do Lucide com cor e tamanho corretos */}
          <Menu className="text-white" size={28} strokeWidth={2} />
        </div>
      </header>
    );
    ```

    REGRAS CRITICAS:
    - fontFamily: 'var(--font-nunito)' — NAO 'Arial'. A variavel CSS ja esta disponivel no body via layout.tsx
    - font-extrabold (800) — peso padrao ate confirmacao via spike se deve ser font-black (900)
    - UneIcon vem de '@/assets/une-icon.svg' — import SVGR, nao de lucide-react
    - CardHeader e named export (nao default) — padrao do projeto per CLAUDE.md global
    - Arquivo fica em src/components/ (nao src/features/) — componente compartilhado
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - src/components/card-header.tsx existe com named export CardHeader
    - Importa UneIcon de '@/assets/une-icon.svg' (nao Globe de lucide-react)
    - Logo usa fontFamily: 'var(--font-nunito)'
    - Subtitulo "Documento Nacional do Estudante" presente como span
    - Menu do Lucide presente com size=28, strokeWidth=2, className="text-white"
    - Compila sem erros TypeScript
  </done>
</task>

<task id="02-02-T2" type="auto">
  <name>Integrar CardHeader em page.tsx substituindo o header inline</name>
  <files>
    - src/app/[student]/page.tsx
  </files>
  <action>
    Atualizar src/app/[student]/page.tsx:

    1. Adicionar import do CardHeader no topo (apos imports existentes):
       ```typescript
       import { CardHeader } from '@/components/card-header';
       ```

    2. Remover o import do Globe do lucide-react (linha 1 — `Globe` da destructuring):
       - ANTES: `import { Globe, Menu, Copy, Check } from "lucide-react";`
       - DEPOIS: `import { Copy, Check } from "lucide-react";`
       (Menu tambem pode ser removido se nao usado em outro lugar — verificar)

    3. Substituir o bloco `<header>` inline (linhas 44-59 do arquivo atual) por:
       ```tsx
       <CardHeader />
       ```

    O bloco a ser removido e exatamente:
    ```tsx
    {/* --- HEADER --- */}
    <header className="px-3 pt-2 pb-4 flex justify-between items-center">
      {/* Logo DNE customizado com fonte parecida */}
      <h1
        className="text-white text-4xl font-extrabold tracking-tighter select-none"
        style={{ fontFamily: "Arial, sans-serif" }}
      >
        dne
      </h1>

      <div className="flex items-center gap-4">
        <div className="w-8 h-8 bg-white rounded-full flex items-center justify-center">
          <Globe className="text-[#005c9e]" size={20} strokeWidth={2.5} />
        </div>
        <Menu className="text-white" size={28} strokeWidth={2} />
      </div>
    </header>
    ```

    Resultado esperado no JSX da page.tsx (secao header):
    ```tsx
    {/* --- HEADER --- */}
    <CardHeader />
    ```

    Manter todos os outros blocos de page.tsx intactos (cards, footer, generateStaticParams).
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - page.tsx importa CardHeader de '@/components/card-header'
    - Bloco header inline removido de page.tsx
    - Globe nao mais importado de lucide-react em page.tsx
    - pnpm build completa sem erros
    - Rota /luccas renderiza sem erro 500
  </done>
</task>

## Verification

```bash
# Build completo sem erros
cd /Users/brunolago/Developer/dne-digital && pnpm build

# Verificar que Globe nao aparece mais em page.tsx
grep -n "Globe" /Users/brunolago/Developer/dne-digital/src/app/[student]/page.tsx

# Verificar que CardHeader e importado em page.tsx
grep -n "CardHeader" /Users/brunolago/Developer/dne-digital/src/app/[student]/page.tsx

# Verificar que fonte Nunito esta no componente (nao Arial)
grep -n "nunito\|Arial" /Users/brunolago/Developer/dne-digital/src/components/card-header.tsx
```

## Success Criteria

1. Logo "dne" usa `fontFamily: 'var(--font-nunito)'` — zero Arial inline
2. Subtitulo "Documento Nacional do Estudante" renderiza em texto branco pequeno abaixo do logo
3. Icone UNE e o SVG real de une-icon.svg (nao Globe do Lucide)
4. Menu hamburger branco tamanho 28 presente no lado direito
5. page.tsx usa `<CardHeader />` — sem bloco header inline duplicado
6. `pnpm build` sem erros TypeScript ou de modulo
