---
phase: "02-visual-components"
plan: "02-05"
type: standard
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/card-footer.tsx
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
must_haves:
  - "Botao Certificado e pill azul solido bg-dne-navy (nao transparente com borda)"
  - "Badge Apple Wallet usa next/image com dimensoes corretas w-[160px] h-[46px]"
  - "CardFooter e componente separado em src/components/card-footer.tsx"
  - "page.tsx importa CardFooter e remove o bloco footer inline"
  - "pnpm build sem erros"
---

# Plan 02-05: Implementar CardFooter

## Objective

Extrair o bloco de footer de page.tsx para `src/components/card-footer.tsx`, convertendo o botao Certificado de transparente-com-borda para pill azul solido `bg-dne-navy` (FOOT-01), e validando que o badge Apple Wallet esta correto (FOOT-02).

## Context

- Footer atual em page.tsx (linhas 116-138): botao Certificado com `border border-white/40 backdrop-blur-sm` — deve virar `bg-dne-navy`
- Token `--color-dne-navy: #1B3A6B` ja definido em globals.css — disponivel como `bg-dne-navy`
- Badge Apple Wallet atual: usa `Image` com `className="w-[160px] h-[46px]"` — visualmente correto
  - FOOT-03 (bug `w-[160]` → `w-[160px]`) era o bug original, mas o page.tsx atual JA tem `w-[160px]` correto
  - Verificar no codigo atual antes de assumir o bug ainda existe
- `AppleWalletImage` importado de `@/assets/Add_to_Apple_Wallet_badge.png` — manter este import
- `AppleWallet` component (SVG inline) definido como `AppleWalletBadge` no final de page.tsx — esse componente nao e usado atualmente no JSX principal (e dead code)
- `Check` do lucide-react e usado no botao Certificado — mover para card-footer.tsx
- Componente e RSC puro — sem "use client"

## Tasks

<task id="02-05-T1" type="auto">
  <name>Criar CardFooter com botao Certificado solido e badge Apple Wallet</name>
  <files>
    - src/components/card-footer.tsx
  </files>
  <action>
    Criar src/components/card-footer.tsx:

    ```typescript
    import { Check } from 'lucide-react';
    import Image from 'next/image';
    import AppleWalletImage from '@/assets/Add_to_Apple_Wallet_badge.png';

    /**
     * Footer da carteirinha com botao Certificado (pill azul solido) e badge Apple Wallet.
     * FOOT-01: bg-dne-navy substituindo o transparente com borda do original.
     * FOOT-02: Badge Apple Wallet com dimensoes corretas.
     */
    export const CardFooter = () => (
      <div className="mt-auto px-3 pb-8 w-full flex flex-col gap-4 items-center">
        {/* FOOT-01: Botao Certificado — pill azul solido */}
        <button className="w-[280px] py-3 rounded-full bg-dne-navy text-white flex items-center justify-center gap-2 text-sm font-medium">
          <div className="w-4 h-4 rounded-full border border-white flex items-center justify-center">
            <Check size={10} strokeWidth={4} />
          </div>
          Certificado
        </button>

        {/* FOOT-02: Badge Apple Wallet */}
        <div
          className="h-[46px] rounded-lg transition-colors hover:bg-stone-900 shadow-lg relative overflow-hidden p-0"
          aria-label="Adicionar à Carteira da Apple"
        >
          <Image
            src={AppleWalletImage}
            alt="Apple Wallet"
            width={220}
            height={35}
            className="w-[160px] h-[46px]"
          />
        </div>
      </div>
    );
    ```

    REGRAS CRITICAS:
    - Botao Certificado: `bg-dne-navy` — NAO `border border-white/40 backdrop-blur-sm`
    - `bg-dne-navy` e o token Tailwind do `--color-dne-navy: #1B3A6B` definido em globals.css
    - Badge Apple Wallet: `className="w-[160px] h-[46px]"` — com 'px' explicitico (nao w-[160])
    - Import de AppleWalletImage usa PNG (nao o SVG Apple-Wallet.svg)
    - Named export — padrao do projeto
    - NAO referenciar o componente AppleWallet (apple-wallet.tsx) — usar Image com PNG diretamente
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - src/components/card-footer.tsx existe com named export CardFooter
    - Botao tem className contendo "bg-dne-navy" e nao contendo "border-white/40" nem "backdrop-blur"
    - Image badge tem className="w-[160px] h-[46px]" (com px)
    - Compila sem erros TypeScript
  </done>
</task>

<task id="02-05-T2" type="auto">
  <name>Integrar CardFooter em page.tsx e limpar dead code</name>
  <files>
    - src/app/[student]/page.tsx
  </files>
  <action>
    Atualizar src/app/[student]/page.tsx:

    1. Adicionar import do CardFooter:
       ```typescript
       import { CardFooter } from '@/components/card-footer';
       ```

    2. Substituir o bloco "FOOTER" (linhas 116-138 do arquivo atual):

    ```tsx
    {/* ANTES — remover: */}
    {/* --- FOOTER --- */}
    <div className="mt-auto px-3 pb-8 w-full flex flex-col gap-4 items-center">
      {/* Botão Certificado */}
      <button className="w-[280px] py-3 rounded-full border border-white/40 text-white flex items-center justify-center gap-2 text-sm font-medium backdrop-blur-sm">
        <div className="w-4 h-4 rounded-full border border-white flex items-center justify-center">
          <Check size={10} strokeWidth={4} />
        </div>
        Certificado
      </button>

      {/* Botão Apple Wallet */}
      <div
        className="h-[46px] rounded-lg transition-colors hover:bg-stone-900 shadow-lg relative overflow-hidden p-0"
        aria-label="Adicionar à Carteira da Apple"
      >
        <Image
          src={AppleWalletImage}
          alt="Apple Wallet"
          width={220}
          height={35}
          className="w-[160px] h-[46px]"
        />
      </div>
    </div>
    ```

    ```tsx
    {/* DEPOIS — substituir por: */}
    {/* --- FOOTER --- */}
    <CardFooter />
    ```

    3. Remover imports que ficaram sem uso em page.tsx apos esta e as tarefas anteriores:
       - `Check` do lucide-react (movido para card-footer.tsx)
       - `AppleWalletImage` de '@/assets/Add_to_Apple_Wallet_badge.png' (movido para card-footer.tsx)
       - `AppleWallet` de '@/components/apple-wallet' (era dead code — nunca usado no JSX principal)

    4. Remover o componente `AppleWalletBadge` do final de page.tsx (linhas 144-150) — era dead code:
    ```tsx
    // REMOVER completamente:
    const AppleWalletBadge = () => (
      <AppleWallet
        className="absolute inset-0 w-full h-full block"
        role="img"
        aria-label="Adicionar à Carteira da Apple"
      />
    );
    ```

    5. Verificar que `generateStaticParams` permanece intacto no final do arquivo.

    Apos esta tarefa, page.tsx deve conter apenas:
    - Imports necessarios (lucide-react sem Check, next/image se ainda usado, qrcode.react se ainda em page, componentes)
    - A funcao `StudentPage` com o JSX limpo usando CardHeader, PhotoCard, QrCard, InfoCard, CardFooter
    - A funcao `generateStaticParams`
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - page.tsx importa CardFooter de '@/components/card-footer'
    - Bloco footer inline removido de page.tsx
    - AppleWalletBadge dead code removido
    - Imports orphaos (Check, AppleWalletImage, AppleWallet) removidos de page.tsx
    - generateStaticParams preservado
    - pnpm build sem erros
  </done>
</task>

## Verification

```bash
# Build completo
cd /Users/brunolago/Developer/dne-digital && pnpm build

# Verificar que botao NAO tem mais o estilo transparente
grep -n "backdrop-blur\|border-white/40" /Users/brunolago/Developer/dne-digital/src/components/card-footer.tsx

# Verificar que botao TEM o estilo solido
grep -n "bg-dne-navy" /Users/brunolago/Developer/dne-digital/src/components/card-footer.tsx

# Verificar que page.tsx esta limpo de dead code
grep -n "AppleWalletBadge\|backdrop-blur" /Users/brunolago/Developer/dne-digital/src/app/[student]/page.tsx
```

## Success Criteria

1. Botao Certificado usa `bg-dne-navy` — pill azul solido #1B3A6B (FOOT-01)
2. Botao NAO tem `border border-white/40` nem `backdrop-blur-sm` — visual correto
3. Badge Apple Wallet exibe com `w-[160px] h-[46px]` (FOOT-02)
4. page.tsx usa `<CardFooter />` — sem bloco footer inline
5. Dead code `AppleWalletBadge` removido de page.tsx
6. `pnpm build` sem erros
