---
phase: "02-visual-components"
plan: "02-04"
type: standard
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/info-card.tsx
  - src/app/[student]/page.tsx
autonomous: true
requirements:
  - INFO-01
  - INFO-02
  - INFO-03
  - INFO-04
  - INFO-05
must_haves:
  - "InfoCard exibe nome do estudante em bold com cor #555555"
  - "InfoCard exibe exatamente 5 campos: Ins. Ensino, Curso, CPF, Data de Nasc, Validade — sem campos Nivel e RG"
  - "Cada campo usa InfoRow existente com label em bold e valor em gray-500"
  - "Card tem bg-white rounded-2xl p-5 shadow-sm — border-radius e padding corretos"
  - "page.tsx importa InfoCard e remove o bloco info inline"
---

# Plan 02-04: Implementar InfoCard

## Objective

Extrair o bloco de informacoes do estudante de page.tsx para `src/components/info-card.tsx`, garantindo que os 5 campos corretos do app original sejam exibidos (sem "Nivel de Ensino" e "RG" que ja foram removidos do tipo Student), reutilizando o componente `InfoRow` existente.

## Context

- `src/components/info-row.tsx` ja existe e implementa o pattern label bold + valor cinza — reutilizar sem modificar
- Card de info atual em page.tsx (linhas 99-113): ja tem a estrutura correta com 5 InfoRows
- Campos corretos: `instituicao`, `curso`, `cpf`, `nascimento`, `validade` — ja no tipo Student apos Phase 1
- Campos removidos: `nivel` e `rg` — ja nao existem no DATABASE nem no tipo Student (DATA-01, DATA-03 completos)
- INFO-04 ja esta substantivamente resolvido pela Phase 1 — apenas garantir que nao aparecem na extracao
- Cor do nome: `text-[#555555]` (hexadecimal inline) — manter como esta, color token `--color-dne-text` e `#5C5C5C`
  - Nota: #555555 vs #5C5C5C — diferenca minima; manter o valor existente do page.tsx (#555555)
- Componente e RSC puro — sem "use client"

## Tasks

<task id="02-04-T1" type="auto">
  <name>Criar InfoCard reutilizando InfoRow com 5 campos corretos</name>
  <files>
    - src/components/info-card.tsx
  </files>
  <action>
    Criar src/components/info-card.tsx:

    ```typescript
    import { InfoRow } from '@/components/info-row';
    import type { Student } from '@/constants';

    interface InfoCardProps {
      student: Student;
    }

    /**
     * Card de informacoes do estudante com nome e 5 campos do app DNE original.
     * Campos exibidos: Ins. Ensino, Curso, CPF, Data de Nasc, Validade.
     * Campos excluidos: Nivel de Ensino e RG (nao aparecem no app original — INFO-04).
     */
    export const InfoCard = ({ student }: InfoCardProps) => (
      <div className="mx-3 mt-3 bg-white rounded-2xl p-5 shadow-sm relative">
        {/* INFO-02: Nome do estudante em bold com tamanho e cor corretos */}
        <h2 className="text-[#555555] font-bold text-lg mb-3 leading-tight">
          {student.nome}
        </h2>

        {/* INFO-03 + INFO-05: 5 campos com pattern label bold + valor cinza */}
        <div className="space-y-[5px] text-[13px] leading-snug">
          <InfoRow label="Ins. Ensino" value={student.instituicao} />
          <InfoRow label="Curso" value={student.curso} />
          <InfoRow label="CPF" value={student.cpf} />
          <InfoRow label="Data de Nasc" value={student.nascimento} />
          <InfoRow label="Validade" value={student.validade} />
        </div>
      </div>
    );
    ```

    REGRAS CRITICAS:
    - Importar Student de '@/constants' (nao recriar o tipo)
    - Prop e `student: Student` (objeto completo) — evita prop drilling de 5+ campos individuais
    - Exatamente 5 InfoRows — nao adicionar nivel, rg, ou qualquer outro campo
    - Labels EXATOS: "Ins. Ensino", "Curso", "CPF", "Data de Nasc", "Validade" — copiar do page.tsx atual
    - Classe do card: `mx-3 mt-3 bg-white rounded-2xl p-5 shadow-sm relative` — identica ao atual em page.tsx
    - Named export — padrao do projeto
    - NAO modificar info-row.tsx — reutilizar como esta
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - src/components/info-card.tsx existe com named export InfoCard
    - Importa InfoRow de '@/components/info-row' e Student de '@/constants'
    - Tem exatamente 5 InfoRow (instituicao, curso, cpf, nascimento, validade)
    - Nenhuma referencia a campos nivel ou rg
    - Compila sem erros TypeScript
  </done>
</task>

<task id="02-04-T2" type="auto">
  <name>Integrar InfoCard em page.tsx substituindo o bloco inline</name>
  <files>
    - src/app/[student]/page.tsx
  </files>
  <action>
    Atualizar src/app/[student]/page.tsx:

    1. Adicionar import do InfoCard:
       ```typescript
       import { InfoCard } from '@/components/info-card';
       ```

    2. Substituir o bloco "CARD DE INFORMACOES" (linhas 99-113 do arquivo atual) por:

    ```tsx
    {/* ANTES — remover: */}
    {/* --- CARD DE INFORMAÇÕES --- */}
    <div className="mx-3 mt-3 bg-white rounded-2xl p-5 shadow-sm relative">
      {/* Nome Principal */}
      <h2 className="text-[#555555] font-bold text-lg mb-3 leading-tight">
        {data.nome}
      </h2>

      {/* Lista de Dados - Usando grid para alinhamento perfeito */}
      <div className="space-y-[5px] text-[13px] leading-snug">
        <InfoRow label="Ins. Ensino" value={data.instituicao} />
        <InfoRow label="Curso" value={data.curso} />
        <InfoRow label="CPF" value={data.cpf} />
        <InfoRow label="Data de Nasc" value={data.nascimento} />
        <InfoRow label="Validade" value={data.validade} />
      </div>
    </div>
    ```

    ```tsx
    {/* DEPOIS — substituir por: */}
    {/* --- CARD DE INFORMAÇÕES --- */}
    <InfoCard student={data} />
    ```

    3. Remover o import de InfoRow de page.tsx (agora usado dentro de info-card.tsx):
       - Verificar se InfoRow ainda e importado em page.tsx — se sim, remover da linha de import
       - ANTES: `import { InfoRow } from "@/components/info-row";`
       - DEPOIS: linha removida (InfoRow agora e dependencia de info-card.tsx, nao de page.tsx)

    4. Manter todos os outros blocos de page.tsx intactos.
  </action>
  <verify>
    <automated>cd /Users/brunolago/Developer/dne-digital && pnpm build 2>&1 | grep -E "(error|Error|compiled)" | tail -5</automated>
  </verify>
  <done>
    - page.tsx importa InfoCard de '@/components/info-card'
    - page.tsx nao importa mais InfoRow diretamente
    - Bloco de info inline removido de page.tsx
    - pnpm build sem erros
    - Rota /luccas renderiza card de informacoes com 5 campos
  </done>
</task>

## Verification

```bash
# Build completo
cd /Users/brunolago/Developer/dne-digital && pnpm build

# Verificar que InfoRow nao esta sendo importado diretamente em page.tsx
grep -n "InfoRow" /Users/brunolago/Developer/dne-digital/src/app/[student]/page.tsx

# Verificar que InfoCard tem exatamente 5 InfoRows
grep -c "InfoRow" /Users/brunolago/Developer/dne-digital/src/components/info-card.tsx

# Verificar ausencia de campos proibidos
grep -n "nivel\|rg\b" /Users/brunolago/Developer/dne-digital/src/components/info-card.tsx
```

## Success Criteria

1. InfoCard tem exatamente 5 InfoRows com labels: "Ins. Ensino", "Curso", "CPF", "Data de Nasc", "Validade"
2. Nenhum campo "Nivel de Ensino" ou "RG" no componente (INFO-04)
3. Nome do estudante em `text-[#555555] font-bold text-lg` (INFO-02)
4. Card tem `bg-white rounded-2xl p-5 shadow-sm` — border-radius e padding corretos (INFO-01)
5. InfoRow reutilizado sem modificacao — pattern label bold + valor cinza preservado (INFO-05)
6. page.tsx usa `<InfoCard student={data} />` — sem bloco inline
